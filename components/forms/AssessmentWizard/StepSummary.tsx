"use client";
/** StepSummary — bespoke wizard step (genuinely different from a rating item). TODO. */
export default function StepSummary() {
  return (
    <div data-step="StepSummary" className="p-6">
      <p className="text-navy/60">StepSummary (TODO)</p>
    </div>
  );
}
