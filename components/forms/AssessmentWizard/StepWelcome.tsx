"use client";
/** StepWelcome — bespoke wizard step (genuinely different from a rating item). TODO. */
export default function StepWelcome() {
  return (
    <div data-step="StepWelcome" className="p-6">
      <p className="text-navy/60">StepWelcome (TODO)</p>
    </div>
  );
}
