"use client";
/** StepEquipment — bespoke wizard step (genuinely different from a rating item). TODO. */
export default function StepEquipment() {
  return (
    <div data-step="StepEquipment" className="p-6">
      <p className="text-navy/60">StepEquipment (TODO)</p>
    </div>
  );
}
