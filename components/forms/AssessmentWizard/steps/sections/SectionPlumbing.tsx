"use client";
import SectionShell from "../../shared/SectionShell";

/** Plumbing, Valves & Seals — shared shell (vacuum breaker + autofill photos). */
export default function SectionPlumbing() {
  return <SectionShell sectionId="plumbing" />;
}
