"use client";
import SectionShell from "../../shared/SectionShell";

/** Cleaning Equipment & Vacuum — shared shell, photos added as needed. */
export default function SectionCleaning() {
  return <SectionShell sectionId="cleaning" />;
}
