"use client";
import SectionShell from "../../shared/SectionShell";

/** Decking, Coping & Surroundings — shared shell, photos added as needed. */
export default function SectionDecking() {
  return <SectionShell sectionId="decking" />;
}
