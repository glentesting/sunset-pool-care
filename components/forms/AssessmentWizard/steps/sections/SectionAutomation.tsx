"use client";
import SectionShell from "../../shared/SectionShell";

/** Automation & Controls — shared shell, photos added as needed. */
export default function SectionAutomation() {
  return <SectionShell sectionId="automation" />;
}
