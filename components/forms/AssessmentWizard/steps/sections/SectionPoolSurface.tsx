"use client";
import SectionShell from "../../shared/SectionShell";

/**
 * Pool Surface & Interior Finish — checklist only.
 * Interior Lights moved to Automation, Controls & Electrical (spec 1.5).
 */
export default function SectionPoolSurface() {
  return <SectionShell sectionId="surface" />;
}
