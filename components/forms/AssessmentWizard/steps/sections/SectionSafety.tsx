"use client";
import SectionShell from "../../shared/SectionShell";

/** Safety Equipment — shared shell, photos added as needed. */
export default function SectionSafety() {
  return <SectionShell sectionId="safety" />;
}
