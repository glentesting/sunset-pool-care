# SPC website — environment variables
# Copy to .env.local and fill in. NEVER commit real values.
# Everything here is server-side only. Do NOT prefix any of these with NEXT_PUBLIC_.

# --- HubSpot (direct REST API) ---
HUBSPOT_PRIVATE_APP_TOKEN=
# Portal 244173183. Scopes needed: crm.objects.contacts read/write (+ tasks).

# --- Assessment submit (via Make.com webhook) ---
MAKE_ASSESSMENT_WEBHOOK_URL=
# Make scenario that receives the assessment on submit and creates the HubSpot
# ticket. Body: full assessment (photo base64 stripped) + top-level pdf_url +
# ticket_body.

# --- Supabase (assessment PDF storage) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# On submit the PDF is uploaded to the PRIVATE "assessment-pdfs" bucket and a
# 1-year signed URL is passed to Make as pdf_url. Service role key is server-only.
# Smoke-test after setting these: GET /api/health/supabase
# HEALTHCHECK_TOKEN=
# Optional. If set, /api/health/* requires ?token= (or x-healthcheck-token header).

# --- Google Drive (service account) ---
GOOGLE_SERVICE_ACCOUNT_JSON=
GDRIVE_ASSESSMENT_FOLDER_ID=
# Service account JSON (stringified) + target folder for assessment PDFs.

# --- Claude API (report wording: notes polish + summary paragraph) ---
ANTHROPIC_API_KEY=
# Server-side only. Used to rewrite the customer-facing WORDING of the report
# (never the findings). If absent, the report falls back to raw notes and omits
# the summary — it never blocks.

# --- Analytics (client-safe, added in content phase) ---
# NEXT_PUBLIC_GA4_ID=
# NEXT_PUBLIC_META_PIXEL_ID=
